# nodejs-restful-api

Basic CRUD express rest api with authentication via JWT.

Features:
- CRUD of a generic model.
- Basic user authentication.
- Deleted data remains in database marked as deleted to allow recovery.
- Scheduler to remove deleted data from database.